import { HandleInputDirective } from './handle-input.directive';

describe('HandleInputDirective', () => {
  it('should create an instance', () => {
    const directive = new HandleInputDirective();
    expect(directive).toBeTruthy();
  });
});
